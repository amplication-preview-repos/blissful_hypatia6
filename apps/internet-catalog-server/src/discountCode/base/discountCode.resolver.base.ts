/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { DiscountCode } from "./DiscountCode";
import { DiscountCodeCountArgs } from "./DiscountCodeCountArgs";
import { DiscountCodeFindManyArgs } from "./DiscountCodeFindManyArgs";
import { DiscountCodeFindUniqueArgs } from "./DiscountCodeFindUniqueArgs";
import { CreateDiscountCodeArgs } from "./CreateDiscountCodeArgs";
import { UpdateDiscountCodeArgs } from "./UpdateDiscountCodeArgs";
import { DeleteDiscountCodeArgs } from "./DeleteDiscountCodeArgs";
import { User } from "../../user/base/User";
import { Product } from "../../product/base/Product";
import { DiscountCodeService } from "../discountCode.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => DiscountCode)
export class DiscountCodeResolverBase {
  constructor(
    protected readonly service: DiscountCodeService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "DiscountCode",
    action: "read",
    possession: "any",
  })
  async _discountCodesMeta(
    @graphql.Args() args: DiscountCodeCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [DiscountCode])
  @nestAccessControl.UseRoles({
    resource: "DiscountCode",
    action: "read",
    possession: "any",
  })
  async discountCodes(
    @graphql.Args() args: DiscountCodeFindManyArgs
  ): Promise<DiscountCode[]> {
    return this.service.discountCodes(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => DiscountCode, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "DiscountCode",
    action: "read",
    possession: "own",
  })
  async discountCode(
    @graphql.Args() args: DiscountCodeFindUniqueArgs
  ): Promise<DiscountCode | null> {
    const result = await this.service.discountCode(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => DiscountCode)
  @nestAccessControl.UseRoles({
    resource: "DiscountCode",
    action: "create",
    possession: "any",
  })
  async createDiscountCode(
    @graphql.Args() args: CreateDiscountCodeArgs
  ): Promise<DiscountCode> {
    return await this.service.createDiscountCode({
      ...args,
      data: {
        ...args.data,

        user: args.data.user
          ? {
              connect: args.data.user,
            }
          : undefined,

        product: args.data.product
          ? {
              connect: args.data.product,
            }
          : undefined,
      },
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => DiscountCode)
  @nestAccessControl.UseRoles({
    resource: "DiscountCode",
    action: "update",
    possession: "any",
  })
  async updateDiscountCode(
    @graphql.Args() args: UpdateDiscountCodeArgs
  ): Promise<DiscountCode | null> {
    try {
      return await this.service.updateDiscountCode({
        ...args,
        data: {
          ...args.data,

          user: args.data.user
            ? {
                connect: args.data.user,
              }
            : undefined,

          product: args.data.product
            ? {
                connect: args.data.product,
              }
            : undefined,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => DiscountCode)
  @nestAccessControl.UseRoles({
    resource: "DiscountCode",
    action: "delete",
    possession: "any",
  })
  async deleteDiscountCode(
    @graphql.Args() args: DeleteDiscountCodeArgs
  ): Promise<DiscountCode | null> {
    try {
      return await this.service.deleteDiscountCode(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => User, {
    nullable: true,
    name: "user",
  })
  @nestAccessControl.UseRoles({
    resource: "User",
    action: "read",
    possession: "any",
  })
  async getUser(@graphql.Parent() parent: DiscountCode): Promise<User | null> {
    const result = await this.service.getUser(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => Product, {
    nullable: true,
    name: "product",
  })
  @nestAccessControl.UseRoles({
    resource: "Product",
    action: "read",
    possession: "any",
  })
  async getProduct(
    @graphql.Parent() parent: DiscountCode
  ): Promise<Product | null> {
    const result = await this.service.getProduct(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
